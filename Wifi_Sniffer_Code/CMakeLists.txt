# Root CMakeLists.txt for ESP-IDF project
cmake_minimum_required(VERSION 3.5)

# Set the project name
project(Wifi_Sniffer)

# Include ESP-IDF build system
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
